NEXT_PUBLIC_PARLANT_API_URL=http://localhost:8080
NEXT_PUBLIC_AGENT_ID=your-agent-id
ANALYZE=false # When set to true, it enables @next/bundle-analyzer, which creates visual charts of your JavaScript bundle sizes during the build process. This helps you identify large dependencies to optimize performance.
